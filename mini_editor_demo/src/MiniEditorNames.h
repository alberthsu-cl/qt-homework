#pragma once

struct MiniEditorNames
{
    const char* window       = "miniEditor.window";
    const char* property     = "miniEditor.property";
    const char* statusLabel  = "miniEditor.statusLabel";
    const char* previewImage = "miniEditor.previewImage";
    const char* importButton = "miniEditor.importButton";
    const char* addButton    = "miniEditor.addButton";
    const char* playButton   = "miniEditor.playButton";
    const char* stopButton   = "miniEditor.stopButton";
    const char* splitButton  = "miniEditor.splitButton";
    const char* resetButton  = "miniEditor.resetButton";
};

static const MiniEditorNames MiniEditorName;
